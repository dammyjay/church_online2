<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    
    <section class="section">
      <h2><%= video.title %></h2>
      <% if (embedUrl) { %>
      <div style="margin: 20px 0">
        <iframe
          width="100%"
          height="400"
          src="<%= embedUrl %>"
          frameborder="0"
          allowfullscreen
        >
        </iframe>
      </div>
      <% } else { %>
      <p>Video link is invalid.</p>
      <% } %>

      <p><%= video.description %></p>
      <small
        >Posted on <%= new Date(video.created_at3).toLocaleString() %></small
      >

      <div style="margin-top: 20px">
        <a href="/videos" class="login-button">← Back to All Videos</a>
      </div>
    </section>
  </body>
</html>
